async function sendEmail(event) {
    const message = {
      from: "orans-test@oransonline.com",
      to: "pross888@gmail.com",
      subject: "Probando orans new.svelte 3",
      text: "Hola este es un mail hermoso!!!"
    };
    let response = await api.post(`email`, message);
    response.ok
      ? console.log("email sent successfully")
      : console.log("email error, view logs on server");
  }